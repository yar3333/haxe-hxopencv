#include "abstract.hpp"

////////////////////////////////////////////////////////////////////

DEFINE_KIND(k_IplImage);
DEFINE_KIND(k_Mat);

////////////////////////////////////////////////////////////////////

void deleteObject(value obj)
{
	delete val_data(obj);
}
